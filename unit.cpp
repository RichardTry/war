#include "unit.h"

void Unit::update(const float & dt, std::vector<UnitContent> & unitlib)
{

}

void Unit::render(Ogre::RenderTarget * target, std::vector<UnitContent> & unitlib)
{
    // RectangleShape unit(Ogre::Vector2f(unitlib[contentID].spriteSize.x, unitlib[contentID].spriteSize.y));
    // unit.setTexture(&unitlib[contentID].texture);
    // unit.setPosition(position.x, position.y);
    // target->draw(unit);
}
